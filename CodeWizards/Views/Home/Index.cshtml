<html>
<head>
    <style>
        @@import url('https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,200;0,300;0,400;1,200&display=swap');

        img {
            display: block;
            margin-left: auto;
            margin-right: auto;
            margin-top: 3%;
            width: 46%;
        }

        body {
            background: rgb(192,231,240);
            background: radial-gradient(circle, rgba(192,231,240,1) 0%, rgba(99,173,245,1) 100%);
        }

        body{
  text-align:center;
  box-sizing:border-box;
/*   position:relative; */
}

.box{
  position:absolute;
  top:85%;
  left:50%;
  transform : translate(-50% ,-50%);
}

.btn:link,
.btn:visited{
  text-decoration: none;
  text-transform:uppercase;
  position:relative;
  top:0;
  left:0;
  padding:20px 40px;
  border-radius:20px;
  display:inline-block;
  transition: all .5s;
}

        .btn-white {
            background: #0F3643;
            color: #000;
        }

.btn:hover{
   box-shadow:0px 10px 10px rgba(0,0,0,0.2);
   transform : translateY(-3px);
}

.btn:active{
  box-shadow:0px 5px 10px rgba(0,0,0,0.2)
  transform:translateY(-1px);
}

.btn-bottom-animation-1{
  animation:comeFromBottom 1s ease-out .8s;
}

.btn::after{
  content:"";
  text-decoration: none;
  text-transform:uppercase;
  position:absolute;
  width:100%;
  height:100%;
  top:0;
  left:0;
  border-radius:20px;
  display:inline-block;
  z-index:-1;
  transition: all .5s;
}

        .btn-white::after {
            background: #0F3643;
        }

.btn-animation-1:hover::after {
    transform: scaleX(1.4) scaleY(1.6);
    opacity: 0;
}

@@keyframes comeFromBottom{
  0%{
    opacity:0;
    transform:translateY(40px);
  }
  100%{
    opacity:1;
    transform:translateY(0);
  }
}
    </style>
</head>
<body>
    <img src="CovidHelperLogo.png" alt="nesto" />

    <div class="box">
        <a asp-controller="Cards" asp-action="Cards" class="btn btn-white btn-animation-1"><b style="color:#fff; font-family: 'Poppins'">Potraži pomoć</b></a>
    </div>
</body>
</html>


